# Auto-provisioned datasource — Grafana connects to Loki on startup, no manual
# UI setup. Add Prometheus here later the same way for metrics.
apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
